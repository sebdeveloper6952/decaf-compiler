#include <iostream>
#include <fstream>
#include <vector>

#pragma once

class Utils
{
    public:
        static int write_to_file(
            std::string name, 
            std::string
        );
    
};
