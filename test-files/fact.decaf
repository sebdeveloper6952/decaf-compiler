class Program
{
    int fact(int a)
    {
        int t;

        if (a <= 1)
        {
            return 1;
        }

        t = fact(a - 1);

        return a * t;
    }

    void print_int(int a) {}

    void main()
    {
        int x;
        int res;

        x = 8;
        res = fact(x);

        print_int(res);
    }
}